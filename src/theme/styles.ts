const styles = {
  global: {
    body: {
      fontSize: "md",
      "& #__next": {
        display: "flex",
        flexFlow: "column nowrap",
        minHeight: "100vh",
        transition: "0.5s ease-out",
        width: "100%"
      }
    }
  }
}

export default styles
